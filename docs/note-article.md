# 【Obsidian整理が10分で終わる】1,000個のノートを"自動で"最適化するデスクトップアプリを作った — Obsidian Optimizer v4.3

Obsidianのノートが増えすぎて、もう手がつけられない。

「あのメモ、どこに書いたっけ？」と検索しても出てこない。リンクを貼ったはずなのに青文字が赤文字に変わっている。MOC（Map of Content）を作ろうと思って3ヶ月が経った。空のノートや「無題」のノートが何十個も転がっている——。

Obsidianは最高のノートアプリです。それは間違いない。

でも、ノートが500個、1,000個、3,000個と増えていくと、**Vault全体が「知識のゴミ屋敷」になっていく**という現実があります。どれだけ良いノートを書いても、それが他のノートとつながっていなければ、ただのテキストファイルの山です。

私自身、Obsidianを3年以上使い続け、Vault内のノートは4,000を超えました。ある日、自分のVaultを冷静に見て気づいたんです。

**「このVault、半分以上が死んでいる」**と。

孤立したノート。切れたリンク。空のファイル。一度も振り返られないメモ。丁寧に書いたのに、どのMOCにも属していない良質なノート——。

手動で整理しようとしました。丸1日かけて200個のノートを確認して、心が折れました。

だから、**自動でやってくれるツールを作りました。**

---

## Obsidian Optimizer とは

**Obsidian Optimizer**は、Obsidian Vaultを自動で分析・最適化するデスクトップアプリケーションです。

ボタンひとつでVault全体をスキャンし、**孤立ノート**、**壊れたリンク**、**ゴミファイル**、**放置ノート**、**重複候補**、**孤立画像**を検出。さらに**MOCの自動生成**、**ナレッジグラフ分析**、**構造テンプレートの適用**まで、Vault管理に必要なすべてを1つのアプリに統合しました。

Mac（Apple Silicon対応）とWindows（64bit）の両方で動作します。

**【🖼️ 画像①: アプリのダッシュボード全体スクリーンショット】**
*（ヒートマップ、健全度スコア、統計カードが表示されている状態）*

---

## あなたのVault、こんな状態になっていませんか？

Obsidianを長く使っている人ほど、以下の症状に心当たりがあるはずです。

### 症状1: ノートの孤立化

「書いたけど、どこにもリンクされていないノート」が大量にある状態。

Obsidianの真価は双方向リンクにあります。でも、ノートを書くときに毎回「このノートはどこにリンクすべきか？」を考えるのは面倒です。結果として、**書きっぱなしのノートが増えていく**。

Obsidian Optimizerは、Vault全体のリンク構造を解析し、**どのノートからもリンクされず、自分からも他のノートにリンクしていない「完全孤立ノート」を自動検出**します。

検出した孤立ノートは、ワンクリックで適切なMOCに接続できます。キーワードベースの自動分類ルールが、ノートの内容に合ったMOCを提案してくれます。

### 症状2: リンク切れの放置

ノートをリネームしたり移動したりすると、リンクが切れます。Obsidianは[[リンク先]]をファイル名で解決するため、ファイル名が変わるとリンクが壊れるのです。

数個のリンク切れなら手動で直せますが、数十個、数百個となると現実的ではありません。

Obsidian Optimizerは壊れたリンクを検出するだけでなく、**Levenshtein距離（編集距離）と部分一致アルゴリズムを使って、正しいリンク先の候補を自動提案**します。「このリンク、たぶんこっちのファイルじゃないですか？」と教えてくれるわけです。

### 症状3: ゴミファイルの蓄積

Obsidianでは、新規ノートを開いて何も書かずに閉じると空のファイルが残ります。「とりあえず作ったけど書かなかったノート」「メモを別のノートに移動して空になったノート」——こうした**実質的にゴミのファイルがVaultに堆積していく**のです。

Obsidian Optimizerのゴミファイル検出は、単にファイルサイズだけでなく、**フロントマターとコメントを除去した後の実質的なテキスト量**で判定します。フロントマターだけ残っているような「見かけ上は中身があるけど実質空」のファイルも見逃しません。

検出基準（最小文字数、最小バイト数、無題キーワード）はすべてカスタマイズ可能です。

### 症状4: MOCが作れない / 作っても更新できない

MOC（Map of Content）は、ノートを整理する最も強力な仕組みです。テーマごとの「目次ノート」を作り、関連ノートへのリンクを集約する。これがあるだけで、Vaultの見通しは劇的に改善します。

でも、**MOCの作成と維持は面倒**です。

どのノートをどのMOCに入れるか、手動で判断して、リンクを書いて、定期的に新しいノートを追加して——。ノートが1,000個を超えると、この作業だけで何時間もかかります。

Obsidian Optimizerは、**5種類のテンプレート + AI自動分類で、MOCを数秒で生成**します。しかも、新しいノートを追加したらMOCも自動で更新できます。

### 症状5: Vaultの全体像が見えない

ノートが増えるほど、「自分のVaultが今どういう状態なのか」が分からなくなります。

- 全部で何個のノートがあるのか
- どれくらいが孤立しているのか
- どのフォルダが肥大化しているのか
- 最近の更新傾向はどうなっているのか

こうした**メタ情報**を把握できていないと、整理の方針も立てられません。

Obsidian Optimizerのダッシュボードは、Vaultの「健康診断書」です。健全度スコア（0〜100）、統計カード、更新ヒートマップ、フォルダ分布チャートで、Vaultの全体像をひと目で把握できます。

**【🖼️ 画像②: 悩み→解決のBefore/After図解】**
*（左: 散らかったVault → 中央: Obsidian Optimizer → 右: 整理されたVault）*

---

## 機能紹介

ここからは、Obsidian Optimizerの各機能を詳しく紹介します。

### 1. ダッシュボード — Vaultの「健康診断書」

アプリを起動して最初に表示されるのが、ダッシュボードです。

#### 健全度スコア

Vaultの整理状態を0〜100の数値で表示します。孤立ノート、ゴミファイル、壊れたリンク、重複候補、放置ノートの数から総合スコアを算出。数値が高いほど健全なVaultです。

前回のスキャン結果との比較バッジも表示されるので、「前回より孤立ノートが3個増えた」「ゴミファイルが2個減った」といった変化が即座に分かります。

#### 統計カード

以下の情報がカード形式で一覧表示されます:

- 総ノート数
- MOC数
- リンク総数
- 壊れたリンク数
- 孤立ノート数
- 放置ノート数
- ゴミファイル数
- 重複候補数
- 孤立画像数

#### 更新ヒートマップ（過去1年）

GitHubの「草」と同じビジュアルで、過去1年間のノート更新状況を可視化します。**日付をクリックすると「タイムマシン」機能が起動し、その日に作成・更新されたノートの一覧が表示**されます。

「先月はあまりノートを書いていなかったな」「この週はやたら更新が多い」といった自分の執筆パターンが視覚的に把握できます。

#### フォルダ分布

円グラフでVault内のフォルダサイズ比率を可視化。特定のフォルダが肥大化していないかを確認できます。

#### 推奨アクション

スキャン結果に基づいて、「次に何をすべきか」をボタン形式で提案します。「孤立ノートが15個あります → スキャン結果を確認」「ゴミファイルが8個あります → 最適化を実行」のように、アクションに直結した導線を提供します。

**【🖼️ 画像③: ダッシュボード — 健全度スコア + 統計カードのスクリーンショット】**

**【🖼️ 画像④: ヒートマップ拡大 + タイムマシン機能のスクリーンショット】**
*（日付をクリック → その日の変更ノート一覧が表示されている状態）*

---

### 2. ワンクリックスキャン — 6種類の問題を同時検出

ダッシュボードの「スキャン」ボタンを押すと、Vault全体を解析して以下の6カテゴリの問題を検出します。

#### ⚠️ 孤立ノート

他のノートからリンクされず、自身も他のノートへリンクしていないノート。検出後、ワンクリックでMOCに接続するか、不要であれば削除できます。一括操作にも対応しており、チェックボックスで複数選択して一気にMOC接続することも可能です。

各ノートにはプレビューボタンがついており、中身を確認してから判断できます。

#### 📅 放置ノート

一定期間（デフォルト180日）更新されていないノート。「書いたまま忘れている」ノートを掘り起こします。一括アーカイブ機能で、`99 Archive`フォルダに移動できます。放置日数は設定画面でカスタマイズ可能です。

#### 🗑️ ゴミファイル

実質的に中身のないファイル。判定基準は3つ:
- ファイルサイズが最小バイト数（デフォルト5バイト）未満
- フロントマター・コメント除去後のテキストが最小文字数（デフォルト20文字）未満
- ファイル名に無題キーワード（`untitled`、`無題`など）を含む

処理方法は「完全削除」と「Archiveフォルダに移動」から選択できます。

#### 💔 壊れたリンク

ターゲットファイルが存在しない内部リンク。リンク元ファイル、壊れたリンク先、修復候補（上位3件）を表示。Levenshtein距離と部分一致で候補を算出するため、「ファイル名をちょっと変えただけ」のケースはほぼ正確にマッチします。

「全て修復」ボタンで一括修復も可能です。

#### 📋 重複ノート候補

ファイル名の類似度、ファイルサイズ、コンテンツハッシュから重複の可能性があるノートをペアで表示。マージプレビューを確認してから統合できます。

#### 🖼️ 孤立画像・添付ファイル（v4.3新機能）

どのノートからもリンクされていない画像（PNG, JPG, GIF）、PDF、動画ファイルを検出。Obsidianにスクリーンショットをペーストすると画像ファイルが自動作成されますが、ノートを削除しても画像は残ります。こうした**参照されない添付ファイルがVaultの容量を食っている**ケースは非常に多いです。

**【🖼️ 画像⑤: スキャン結果画面 — 孤立ノート一覧のスクリーンショット】**
*（チェックボックスで選択 → 「MOCに接続」ボタンが見えている状態）*

**【🖼️ 画像⑥: 壊れたリンク修復画面のスクリーンショット】**
*（壊れたリンク + 修復候補3件が表示されている状態）*

---

### 3. リアルタイム監視（v4.3新機能）

Vaultの整理は「一度やって終わり」ではありません。毎日ノートを書いていれば、新しい孤立ノートやゴミファイルは日々生まれます。

リアルタイム監視機能は、**「監視開始」ボタンを押すだけで、Vaultのファイル変更をリアルタイムで検出**します。新しいゴミファイルが作られたり、リンクが切れたりしたら即座に通知。

整理を「イベント」ではなく「習慣」に変えるための機能です。

**【🖼️ 画像⑦: リアルタイム監視のUI — 監視中ステータス + イベントログ】**

---

### 4. 変更プレビュー — 実行前に影響範囲を確認

最適化を実行する前に、**何が削除され、何がMOCに接続されるのか**をすべて確認できます。

- 削除予定のゴミファイル一覧（チェックを外して除外可能）
- MOC接続予定のノート一覧（接続先MOCを確認・変更可能）

Vaultのファイルを直接操作するアプリだからこそ、「うっかり大事なノートを消してしまった」は絶対に避けなければなりません。変更プレビューは、安全のための最後の砦です。

---

### 5. 最適化実行

プレビューで確認したら、「最適化」タブで実行します。

実行オプション:
- 🗑️ ゴミファイルを削除: ON/OFF切り替え
- 🔗 孤立ノートをMOCへ接続: ON/OFF切り替え

実行中はリアルタイムで進行状況が表示され、キャンセルも可能。完了後は削除ファイル数・接続ノート数のサマリーが表示されます。

**削除前には自動でバックアップが作成される**ので、万が一の場合も復元できます。

---

ここまでが、Obsidian Optimizerの「整理・最適化」機能です。

ここからは、このアプリの**真骨頂**——**MOC自動生成**、**ナレッジグラフ分析**、**構造テンプレート**について、さらに詳しく紹介します。

また、**Mac版・Windows版のダウンロードリンク**と**詳細な使い方ガイド**も、この先に含まれています。

---

## ここから先は有料です（¥3,980）

ここまでお読みいただきありがとうございます。

この先には以下の内容が含まれています:

**機能解説:**
- MOC自動生成の全貌（3つの作成モード、スマート提案、一括生成）
- ナレッジグラフ分析（クラスター検出、弱接続ノート、リンク提案）
- 構造テンプレート（PARA / Zettelkasten / Johnny.Decimal）
- 統計・分析（タグ分析、ライティング分析、タグリネーム）
- ノート統合・スマートアーカイブ
- 設定の全オプション解説

**ダウンロード:**
- Obsidian Optimizer v4.3（Mac版 .pkg）
- Obsidian Optimizer v4.3（Windows版 .exe）

**運用ガイド:**
- クイックスタート（3ステップ）
- おすすめ週次・月次運用フロー
- トラブルシューティング

**今後のアップデート:**
- 購入者は今後のアップデート版も無料で受け取れます

---

## 6. MOC自動生成 — ノート整理の「本丸」

MOC（Map of Content）は、Obsidianにおけるノート整理の核心です。

テーマごとの「目次ノート」を作り、関連ノートへのリンクを集約する。これがあるだけで、「あのノートどこだっけ？」はなくなり、Vault全体に構造が生まれます。

でも、MOCを手動で作るのは大変です。

- どのノートをどのMOCに入れるか、ひとつずつ判断する必要がある
- 新しいノートを書いたら、既存のMOCに手動で追加しなければならない
- そもそも「どんなMOCを作るべきか」の設計から考えなければならない

Obsidian Optimizerは、この作業を**3つのモード**で自動化します。

### 6-1. テンプレートモード — 5種類から選ぶだけ

あらかじめ用意された5種類のテンプレートから選ぶだけで、即座にMOCが生成されます。

| テンプレート | 特徴 | こんな人向け |
|---|---|---|
| **標準MOC** | Pinned Notes + Dataviewクエリ付きの本格的なMOC | 万能。迷ったらこれ |
| **シンプルMOC** | フロントマター + リンクリストのみのシンプル構成 | 軽量に始めたい人 |
| **フォルダMOC** | フォルダ内ファイルを自動集約するMOC | フォルダ整理派 |
| **Zettelkasten MOC** | 双方向リンク中心、Backlinks重視の構成 | Zettelkastenユーザー |
| **タグベースMOC** | 特定タグを持つノートを自動収集・統計表示 | タグ活用派 |

すべて内蔵テンプレートとして提供されており、選択するだけですぐに使えます。

テンプレートの中では、以下の変数が自動的に埋め込まれます:

- `{{NAME}}` — MOCのタイトル
- `{{TAGS}}` — フロントマターのタグ
- `{{AUTO_TAGS}}` — 自動推定されたタグ
- `{{AUTO_FOLDERS}}` — 関連フォルダ一覧
- `{{RELATED_MOCS}}` — リンク先の既存MOC
- `{{DESCRIPTION}}` — MOCの説明文（空欄時はトピック名から自動生成）

### 6-2. ウィザードモード — ステップバイステップで迷わない

MOCを初めて作る人や、「何から始めればいいか分からない」人のためのモード。6つのステップで順番に案内します。

**ステップ1**: テンプレートを選ぶ
**ステップ2**: MOCの名前と説明を入力
**ステップ3**: タグとフォルダを設定
**ステップ4**: AIスマート提案で関連ノートを確認
**ステップ5**: プレビューで仕上がりを確認
**ステップ6**: 作成実行

「前へ」「次へ」ボタンで自由に行き来でき、どのステップからでもやり直せます。

**【🖼️ 画像⑧: MOC作成ウィザード画面のスクリーンショット】**
*（ステップ形式のUIが表示されている状態）*

### 6-3. スマート提案モード — AIがノートを自動分類

Obsidian Optimizerの最も強力な機能のひとつです。

MOCの名前、タグ、フォルダを入力すると、**Vault内の全ノートを解析して、そのMOCに関連しそうなノートを自動で提案**します。

アルゴリズムの仕組み:

1. **タグマッチング**: 入力タグを持つノートを抽出
2. **フォルダマッチング**: 対象フォルダ内のノートを抽出
3. **キーワードマッチング**: MOC名に含まれるキーワードで、Vault全体をファジー検索
4. **スコアリング**: 上記3つの結果を統合し、関連度スコアで順位づけ

たとえば、MOC名を「AI Tools」、タグを「#ai」「#llm」と入力すると、Vault内の「GPT-4の使い方」「Claude APIメモ」「Difyワークフロー設計」といったノートが自動で提案されます。

### 6-4. キーワードベース自動分類ルール

Obsidian Optimizerには、ノートを自動でカテゴリ分けするためのルールが組み込まれています。

| カテゴリ | キーワード例 | 生成MOC名 |
|---|---|---|
| AI・LLM | ai, gpt, claude, llm, dify, langchain | MOC - AI Tools & Platforms |
| Threads | threads, スレ | MOC - Threads |
| マーケティング | マーケティング, seo, cvr, コンバージョン | MOC - マーケティング |
| 開発・技術 | python, react, javascript, api | MOC - Dev Skills |
| SNS | twitter, instagram, tiktok | MOC - SNS Strategy |
| ビジネス | ビジネス, 起業, saas | MOC - Business Strategy |
| 学習 | 学習, 勉強, 読書 | MOC - Learning |

これらのルールは設定画面からカスタマイズ可能です。自分のVaultのトピックに合わせて、キーワードとMOC名を自由に追加・編集できます。

### 6-5. 既存MOCの更新 + 一括生成

一度MOCを作って終わりではありません。新しいノートが増えたら、MOCも更新する必要があります。

**既存MOC更新機能**は、すでに作成されたMOCファイルを再分析し、新しく追加すべきノートを自動で提案します。「MOCを作ったけど、その後のメンテナンスが面倒で放置している」という人にとって、この機能だけでも価値があるはずです。

**一括MOC生成機能**は、フォルダを指定するだけで、全フォルダに対してMOCをまとめて作成します。「Vaultの全フォルダにMOCを一気に作りたい」というケースに対応。

**【🖼️ 画像⑨: 生成されたMOCをObsidian上で表示したスクリーンショット】**
*（Dataviewクエリ付きの洗練されたMOCノート）*

---

## 7. ナレッジグラフ分析 — 隠れた関係を発見する

ノートを整理するだけでなく、**ノート間の「見えない関係性」を発見する**のが、ナレッジグラフ分析です。

### 7-1. クラスター検出

共通のタグを持つノート群を自動でグルーピングします。

たとえば、`#marketing`と`#ai`の両方のタグを持つノートが5個あれば、「marketing + ai」クラスターとして検出されます。**自分では意識していなかったトピック同士のつながり**が浮かび上がります。

「マーケティングのノートとAIのノートを組み合わせたら、新しいMOCが作れるかも」——そんな発見のきっかけになります。

### 7-2. 弱接続ノート

リンク数が少ないけれど、内容的に重要そうなノートを検出します。

アルゴリズムは、参照元ノードの重要度（どれだけ多くのノートからリンクされているか）を加重して計算するため、単純なリンク数のカウントではなく、**「リンクされるべきなのに、リンクされていないノート」**を発見します。

### 7-3. リンク提案

「このノートとこのノート、つなげた方がいい」を自動で提案します。

タグの共通性、フォルダの近接性、キーワードの類似性から、不足しているリンク関係を算出。提案されたリンクは、ワンクリックで追加できます。

**【🖼️ 画像⑩: ナレッジグラフ分析画面のスクリーンショット】**
*（クラスター一覧 + リンク提案が表示されている状態）*

---

## 8. グラフビュー — ノートの関係を可視化

ナレッジグラフ分析の結果を、**力学モデルのグラフで可視化**します。

Obsidian標準のグラフビューとの違い:

- **ノードの大きさがリンク数に比例**: 多くのノートからリンクされているノートほど大きく表示される
- **タグ別に色分け**: ノートのタグに応じてノードの色が変わり、クラスターが視覚的に分かる
- **ダブルクリックでObsidianを起動**: グラフ上のノードをダブルクリックすると、そのノートがObsidianで開く
- **ドラッグ操作**: ノードをドラッグして配置を調整可能

**【🖼️ 画像⑪: グラフビューのスクリーンショット】**
*（カラフルなノードが接続されている見栄えの良い状態）*

---

## 9. 構造テンプレート — Vaultをメソッドに準拠させる

「ノートの整理方法は分かったけど、そもそもフォルダ構造をどうすればいいのか分からない」

そんな人のために、世界的に実績のある3つの知識管理メソッドのフォルダ構造を、ワンクリックで適用できます。

### PARAメソッド

Tiago Forteが提唱した知識管理の世界標準。

| フォルダ | 役割 |
|---|---|
| `01 Projects` | 現在進行中のプロジェクト |
| `02 Areas` | 継続的な責任分野（健康、財務、キャリアなど） |
| `03 Resources` | 参考資料、学習ノート |
| `04 Archives` | 完了したもの、不要になったもの |

### Zettelkasten

Niklas Luhmannが実践した「知識カード」方式。フォルダ階層を最小限にし、双方向リンクとタグで構造化するアプローチ。

### Johnny.Decimal

10-19、20-29、30-39...のような10進数命名規則で厳密にカテゴリ分けする方式。大規模Vault向け。

---

**適用フロー**:

1. **「Vault構造を分析」**ボタンで現在のフォルダ構造を検出
2. **推奨テンプレートを提案**（現在の構造に最も近いものを自動判定）
3. **プレビュー**で作成予定フォルダと既存ファイルのマッピングを確認
4. **「適用」**で不足フォルダを作成 + 既存ノートをキーワードマッチングで自動配置

**【🖼️ 画像⑫: 構造テンプレート選択画面のスクリーンショット】**
*（3つのテンプレートカードが並んでいる状態 + PARAの詳細説明）*

---

## 10. 統計・分析 — Vaultの深層を知る

数字で見ると、自分のVaultの意外な姿が見えてきます。

### よく使うタグ TOP 15

使用頻度ランキング形式で表示。「自分はこのトピックについて一番多く書いているのか」という発見があります。クリックすると、そのタグでタグベースMOCの作成画面に直接ジャンプできます。

### レアタグ検出

1回しか使われていないタグの一覧。タグの乱立を防ぎ、統合すべきタグを発見できます。

### ライティング分析（v4.1〜）

月別のノート作成数と単語数合計をグラフで可視化。自分の執筆ペースやトレンドを客観的に把握できます。「今月はノートの数は多いけど、1ノートあたりの文字数が少ない」といった分析が可能。

### タグ名変更・マージ（v4.3新機能）

「同じ意味のタグが2つある」ケースは頻繁に起こります。`#AI`と`#ai`、`#マーケ`と`#マーケティング`——。

タグ名変更機能は、旧タグを新タグに**Vault全体で一括リネーム**します。フロントマターのタグもインラインの`#タグ`も両方更新。変更前にバックアップが自動作成されるので安全です。

### Dataviewレポート生成（v4.3新機能）

Vault内にDataviewクエリ付きの自動更新レポートノートを生成。Obsidian上で常に最新のVault統計を確認できます。

---

## 11. ノート統合 — 安全なマージ

「同じトピックについて2つのノートに分かれて書いてしまった」——よくあることです。

ノート統合機能は、2つのノートを選択して安全にマージします。

1. 2つのノートをドロップダウンから選択
2. マージプレビューを確認（ノートA + セパレータ + ノートB、統合元の注釈付き）
3. 確認後にマージ実行
4. ノートBへのリンクは自動的に新しいマージファイルにリダイレクト
5. バックアップが自動作成

**リンクの自動リダイレクト**がポイントです。ノートBを削除するだけだと、ノートBへのリンクが全て切れてしまいます。Obsidian Optimizerは、リンク元のファイルを自動で更新し、マージ後のファイルを指すように書き換えます。

---

## 12. スマートアーカイブ提案

「このノート、もう使わないかも？」をスコアベースで提案します。

アルゴリズムは2つの要素を見ています:
- **更新頻度**: 最終更新からの経過日数
- **リンク状態**: 他のノートからの参照の有無

両方の加重平均でスコアを算出し、アーカイブ候補をランキング表示。個別プレビューで中身を確認してから、ワンクリックでアーカイブ（`99 Archive`フォルダに移動）できます。

---

## 13. 安全設計 — 「消してしまった」を防ぐ

Vaultのファイルを直接操作するアプリだからこそ、安全設計は最優先事項です。

### 自動バックアップ

設定でONにしておくと（デフォルトON）、**削除・移動・マージの実行前に自動でバックアップが作成**されます。

バックアップ先: `~/.obsidian-optimizer-backups/backup-[タイムスタンプ]/`

### バックアップ管理

設定画面のバックアップ管理セクションから:
- 過去のバックアップ一覧を確認
- 任意のバックアップからVaultを復元
- 不要なバックアップを削除

### 変更プレビュー

最適化実行前に、削除予定ファイル・接続予定ノートの一覧を確認。個別にチェックを外して除外可能。

### Vaultの安全保護

- `isPathInsideVault()`関数により、**Vault外のファイルへの誤操作を防止**
- iCloud同期中（evicted）のファイルは500msのタイムアウトで安全に処理
- 二重起動防止（`requestSingleInstanceLock`）

**【🖼️ 画像⑬: 設定画面 — バックアップ管理セクションのスクリーンショット】**

---

## 14. 設定の全オプション

### Vault管理
- **Vaultスイッチャー**: 複数のVaultをドロップダウンで切り替え
- **追加・削除**: Vaultフォルダの追加・削除

仕事用、プライベート用、研究用——複数のVaultを持っている人も、ひとつのアプリで全て管理できます。

### ゴミ判定ルールのカスタマイズ
- 最小文字数（スライダー）
- 最小バイト数（スライダー）
- 無題判定キーワード（カスタム可能）

### 機能のON/OFF
- ゴミファイル処理: ON/OFF + 処理方法（完全削除 or Archive移動）
- 孤立ノートのMOC接続: ON/OFF

### 自動スキャンレポート（v4.3新機能）
ONにすると、スキャン実行時に自動でレポートをVault内に保存:
`{Vault}/.optimizer-reports/report-[日付].md`

### 言語
日本語 / English / 中文

---

## 対応環境

| 項目 | 内容 |
|------|------|
| **macOS** | Apple Silicon (M1/M2/M3/M4) 対応。macOS 12 以降 |
| **Windows** | 64bit。Windows 10 以降 |
| **Obsidian** | バージョン不問（Vaultフォルダを直接操作するため、Obsidian本体との依存なし） |
| **ディスク容量** | 約200MB |

---

## ダウンロード

以下からMac版・Windows版をダウンロードしてください。

**Mac版（Apple Silicon）**
📎 `Obsidian Optimizer-4.3.0-mac-arm64.pkg`

**Windows版（64bit）**
📎 `Obsidian Optimizer-4.3.0-win-x64.exe`

---

## インストール方法（Mac版）

Mac版は `.pkg` インストーラーで提供しています。以下の手順に沿ってインストールしてください。

### 手順1: pkgファイルを開く

ダウンロードした `Obsidian Optimizer-4.3.0-mac-arm64.pkg` をダブルクリックします。

macOSのインストーラーが起動し、ウィザード形式でインストールが始まります。

**【🖼️ 画像⑮: Macインストーラー起動画面のスクリーンショット】**
*（pkgインストーラーの最初の画面）*

### 手順2: インストール先を確認

インストール先はデフォルトで `/Applications`（アプリケーションフォルダ）です。特別な理由がなければ、そのまま「インストール」をクリックしてください。

管理者パスワードの入力を求められる場合があります。Macのログインパスワードを入力してください。

### 手順3: インストール完了

「インストールが完了しました」と表示されたら成功です。Launchpad またはアプリケーションフォルダから「Obsidian Optimizer」を探してください。

### 手順4: 初回起動時のセキュリティ警告への対処

Obsidian Optimizerは現在Apple Developer Programによるコード署名を行っていないため、初回起動時にmacOSのセキュリティ機能（Gatekeeper）によって警告が表示される場合があります。

これはアプリが危険という意味ではなく、**Apple公式の審査を経ていない個人開発アプリに対して標準的に表示されるもの**です。

#### パターンA:「"Obsidian Optimizer"は、開発元を検証できないため開けません」と表示された場合

1. **システム設定**を開く（画面左上のリンゴマーク → システム設定）
2. 左メニューから**「プライバシーとセキュリティ」**を選択
3. 画面を下にスクロールすると、「"Obsidian Optimizer"は開発元を確認できないため、使用がブロックされました」というメッセージが表示されています
4. **「このまま開く」**ボタンをクリック
5. 確認ダイアログが表示されたら**「開く」**をクリック

**【🖼️ 画像⑯: macOSセキュリティ警告 → システム設定 → 「このまま開く」の手順スクリーンショット】**
*（3枚のスクリーンショットを横並びにして、矢印で手順を示す）*

#### パターンB:「"Obsidian Optimizer"が悪質なソフトウェアかどうかをAppleでは確認できないため、このソフトウェアは開けません」と表示された場合

上記パターンAと同じ手順で解決できます。

#### パターンC: 右クリックで開く方法（代替手段）

1. Finderでアプリケーションフォルダを開く
2. 「Obsidian Optimizer」を**右クリック**（またはControlキーを押しながらクリック）
3. メニューから**「開く」**を選択
4. 確認ダイアログで**「開く」**をクリック

この方法は一度だけ実行すれば、次回からは通常どおりダブルクリックで起動できます。

### 手順5: 起動を確認

アプリが起動すると、ダークテーマのダッシュボード画面が表示されます。左側にサイドバー、中央にメインコンテンツが配置されたレイアウトです。

初回起動時はVaultが未設定のため、まず設定画面（⚙️アイコン）からVaultフォルダを追加してください（詳しくはクイックスタートの章で解説します）。

---

## インストール方法（Windows版）

Windows版は `.exe` インストーラー（NSIS形式）で提供しています。

### 手順1: exeファイルを実行

ダウンロードした `Obsidian Optimizer-4.3.0-win-x64.exe` をダブルクリックします。

### 手順2: SmartScreenの警告への対処

Windows Defender SmartScreenにより、以下のような警告が表示される場合があります。

#### 「WindowsによってPCが保護されました」と表示された場合

これはMicrosoftに登録されていない発行元のアプリに対して標準的に表示されるものです。

1. 青い警告画面の左下にある**「詳細情報」**をクリック
2. アプリ名と発行元の情報が表示されます
3. **「実行」**ボタンをクリック

**【🖼️ 画像⑰: Windows SmartScreen警告 → 「詳細情報」 → 「実行」の手順スクリーンショット】**
*（2枚のスクリーンショットを横並びにして、手順を示す）*

### 手順3: インストールオプションの選択

NSISインストーラーが起動します。

1. **インストール先の選択**: デフォルトのままで問題ありません。変更したい場合は「参照」ボタンからフォルダを選択
2. **「インストール」**をクリック
3. インストールが完了すると、以下が作成されます:
   - **デスクトップショートカット**: ダブルクリックで起動
   - **スタートメニューショートカット**: スタートメニューから起動

**【🖼️ 画像⑱: Windowsインストーラー画面のスクリーンショット】**
*（インストール先選択 + インストールボタン）*

### 手順4: 起動を確認

デスクトップに作成された「Obsidian Optimizer」のショートカットをダブルクリックするか、スタートメニューから起動してください。

### アンインストール方法

不要になった場合のアンインストール方法:

**Mac**: アプリケーションフォルダから「Obsidian Optimizer」をゴミ箱にドラッグ

**Windows**:
1. 設定 → アプリ → アプリと機能
2. 「Obsidian Optimizer」を検索
3. 「アンインストール」をクリック

または、スタートメニューの「Obsidian Optimizer」フォルダ内に「Uninstall」が入っています。

---

## トラブルシューティング（インストール関連）

### Mac: 「アプリケーションが破損しています」と表示される

ダウンロード時にmacOSが付与する「隔離属性」が原因です。ターミナルを開いて以下のコマンドを実行してください:

```
xattr -cr /Applications/Obsidian\ Optimizer.app
```

実行後、再度アプリを起動してください。

### Mac: pkgインストーラーが開けない

macOSのセキュリティ設定により、pkgファイル自体がブロックされる場合があります。

1. pkgファイルを**右クリック** → **「開く」**を選択
2. 確認ダイアログで**「開く」**をクリック

### Windows: インストール後にアプリが起動しない

以下を確認してください:
1. Windows 10 64bit 以降であること（32bit版は非対応）
2. アンチウイルスソフトがブロックしていないか確認
3. 管理者権限で実行を試す（exeを右クリック → 「管理者として実行」）

### 共通: Vaultフォルダが認識されない

Obsidian OptimizerはVaultフォルダ内の `.obsidian` フォルダの存在で判定しています。以下を確認してください:
1. 指定したフォルダがObsidianのVaultフォルダであること
2. フォルダ内に `.obsidian` サブフォルダが存在すること
3. フォルダへのアクセス権限があること（特にiCloud Drive上の場合）

---

## クイックスタート — 3ステップで始める

### ステップ1: Vaultを設定する

アプリを起動したら、設定タブ（⚙️アイコン）を開き、ObsidianのVaultフォルダを追加します。

Vaultフォルダの場所が分からない場合:
- Obsidianを開く → 左下の「Vault名」をクリック → 「Vaultフォルダを開く」

### ステップ2: スキャンを実行する

ダッシュボードの「スキャン」ボタンをクリック。Vault全体が解析され、統計情報と問題一覧が表示されます。

ノートの数によりますが、1,000ノートで約10〜30秒、5,000ノートでも1〜2分程度で完了します。

### ステップ3: 結果を確認して対処する

スキャン結果タブで検出された問題を確認し、対処を選びます:

- 孤立ノート → MOCに接続 or 削除
- ゴミファイル → 削除 or アーカイブ
- 壊れたリンク → 候補から修復
- 放置ノート → アーカイブ or 放置

まずは少量のノートで試して、動作を確認してから一括操作に進むことをおすすめします。

**【🖼️ 画像⑭: クイックスタート3ステップの図解】**
*（ステップ1→2→3 を矢印でつないだ簡潔な図）*

---

## おすすめ運用フロー

### 週次（5〜10分）

1. アプリを起動してスキャン実行
2. ダッシュボードの健全度スコアと前回比バッジを確認
3. 新しい孤立ノートがあればMOCに接続
4. ゴミファイルがあれば削除

### 月次（15〜30分）

1. 週次のフロー +
2. 統計・分析タブでタグの傾向を確認
3. レアタグの統合を検討
4. ライティング分析で執筆パターンを振り返り
5. 必要に応じてMOCの更新（既存MOC更新機能）

### 四半期（30分〜1時間）

1. 月次のフロー +
2. ナレッジグラフ分析でクラスター・弱接続ノートを確認
3. リンク提案を実行
4. 放置ノートのアーカイブ判断
5. 構造テンプレートの見直し

---

## よくある質問

**Q: Obsidian本体がなくても使えますか？**
A: Obsidian Optimizerは、Vaultフォルダ（.mdファイルの集まり）を直接操作します。Obsidian本体がインストールされていなくてもスキャン・分析は可能ですが、「Obsidianで開く」ボタンはObsidianが必要です。

**Q: ノートが消えてしまわないか心配です**
A: デフォルトで自動バックアップが有効になっています。万が一の場合、設定画面のバックアップ管理から復元可能です。また、最適化実行前に変更プレビュー画面で影響範囲を確認できます。

**Q: Obsidian Syncや他のクラウド同期と併用できますか？**
A: はい。Obsidian OptimizerはVaultフォルダ内のファイルを読み書きするだけなので、同期サービスとの競合はありません。ただし、同期中にファイルを操作すると競合が起きる可能性があるため、同期が完了してからスキャンすることをおすすめします。

**Q: iCloud Drive上のVaultでも動作しますか？**
A: はい。iCloud Driveの「ダウンロード待ち」状態のファイル（evicted file）にも対応しています。500msのタイムアウトを設けて安全に処理します。

**Q: Vaultのノートが非常に多い（5,000個以上）のですが、動作が遅くなりませんか？**
A: 数千ノート規模を想定して設計しています。スキャンはファイル単位で非同期処理し、UIのブロックを最小限に抑えています。10,000ノート以上の場合は、初回スキャンに数分かかる場合があります。

**Q: 今後のアップデートはどうなりますか？**
A: 購入者にはアップデート版を無料で提供します。noteの記事を更新してダウンロードリンクを差し替えますので、定期的に記事を確認してください。

---

## 今後のアップデート予定

Obsidian Optimizerは現在も開発を続けています。今後のアップデートで追加予定の機能:

- **AI連携（LLM統合）**: ノート内容の自動要約、関連ノートのセマンティック検索
- **Obsidianプラグインとの連携強化**: Dataview、Templaterとのより深い統合
- **エクスポート機能**: Vault分析レポートのPDF出力
- **パフォーマンス最適化**: 10,000ノート以上の大規模Vaultへの対応強化

---

## おわりに

Obsidianは最高のノートアプリです。でも、**ノートを書く時間と、ノートを整理する時間は別物**です。

書くことに集中したいのに、整理に時間を取られる。MOCを作りたいのに、面倒で後回しにしてしまう。Vaultの全体像が見えず、同じトピックについて何度もノートを作ってしまう——。

Obsidian Optimizerは、**「整理にかかる時間」を「知識を活用する時間」に変える**ためのツールです。

ボタンひとつでスキャン。ワンクリックでMOC生成。自動でリンク修復。リアルタイムで監視。

あなたのVaultを、「知識のゴミ屋敷」から「知識の宝庫」に変えてください。

質問・要望がありましたら、コメント欄でお気軽にどうぞ。

---

**Obsidian Optimizer v4.3 Ultimate Edition**
開発: AI寺子屋 CraftLab
対応: macOS (Apple Silicon) / Windows (64bit)
価格: ¥3,980（買い切り・アップデート無料）

#Obsidian #ナレッジマネジメント #PKM #セカンドブレイン #ノート術 #知識管理 #MOC #Zettelkasten #PARA #デスクトップアプリ #個人開発 #生産性向上 #Vault最適化
